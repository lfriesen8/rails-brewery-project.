<!DOCTYPE html> 
<html>
<head>
  <title>Brewery Explorer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_importmap_tags %> <%# Rails 7 JavaScript setup %>

  <style>
    /* Navbar styles */
    .navbar {
      background: #333;
      padding: 15px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    /* "Die Kneipe" Title */
    .brand-title {
      font-size: 24px;
      color: white;
      font-weight: bold;
      margin-right: 30px;
    }

    /* Navigation links */
    .nav-links {
      display: flex;
      align-items: center;
    }

    .nav-links a {
      font-size: 18px;
      color: white;
      margin-right: 20px;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    /* Hover effect */
    .nav-links a:hover {
      color: #FFD700; /* Gold hover effect */
    }

    /* Click effect */
    .nav-links a:active {
      color: #FF4500; /* Orange click effect */
    }

    /* Search form styles */
    .search-form {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .search-form input[type="text"] {
      padding: 6px;
      font-size: 14px;
      border: none;
      border-radius: 4px;
    }

    .search-form select {
      padding: 6px;
      font-size: 14px;
      border: none;
      border-radius: 4px;
    }

    .search-form button {
      padding: 6px 12px;
      background-color: #FFD700;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .search-form button:hover {
      background-color: #FF4500;
      color: white;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <span class="brand-title">Die Kneipe</span>
    <p>------  Liam Friesen 2025  ------</p>
    <div class="nav-links">
      <a href="<%= root_path %>">Home</a>
    
      <a href="<%= beers_path %>">Beers</a>
     
      <a href="<%= pubs_path %>">Breweries</a>
      
      <a href="<%= about_path %>">About</a>

    </div>

    <br>
    <br>
    <br>
    
    <!-- Search Form -->
    <form action="<%= search_path %>" method="get" class="search-form">
      <input type="text" name="query" placeholder="Search Beers or Breweries..." required>
      <select name="category">
        <option value="beers">Beers</option>
        <option value="breweries">Breweries</option>
      </select>
      <button type="submit">Search</button>
    </form>
  </nav>

  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
